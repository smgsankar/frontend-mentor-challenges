export interface AgeType {
  days: string;
  months: string;
  years: string;
}

export interface DateValidatorPropsType {
  day: number;
  month: number;
  year: number;
  today: Date;
}
